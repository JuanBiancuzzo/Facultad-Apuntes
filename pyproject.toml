[project]
name = "importer"
version = "0.1.0"
description = "Metodo especifico para importar mis archivos a una base de datos"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "docopt-ng>=0.9.0",
    "fastembed>=0.8.0",
    "pyyaml>=6.0.3",
    "tree-sitter>=0.26.0",
    "tree-sitter-markdown>=0.5.1",
]
